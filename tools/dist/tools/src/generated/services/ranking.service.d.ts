import { PATTERN_KEYS_ENUM, QUESTION_ENUM } from 'tools/src/generated/enums';
export declare class RankingService {
    static generate(patternKey: PATTERN_KEYS_ENUM, questionType: number, group: QUESTION_ENUM): number;
}
