{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "lerna run build",
        "format": "lerna run format",
        "core:dev": "yarn workspace @lingo/core run start:dev",
        "core:debug": "yarn workspace @lingo/core run start:debug",
        "core:prod": "yarn workspace @lingo/core run start:prod"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}